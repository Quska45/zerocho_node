쿠키는 name=zerocho 같이 단순한 키 값 쌍이다.
서버로 부터 쿠키가 오면 웹 브라우저는 쿠키를 저장했다가 요청 때마다 쿠키를 동봉해서 보내준다.
브라우저는 쿠키를 자동으로 동봉해서 보내주므로 따로 처리할 필요가 없다.
서버에서 브라우저로 쿠키를 보낼 때만 코드를 작성하면 된다.

쿠키는 요청과 응답의 헤더에 저장된다.
요청과 응답은 헤더와 본문(body)를 가진다.